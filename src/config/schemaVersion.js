/**
 * Schema / migration version for deployment provenance (H-05).
 * Bump when shipping intentional Mongo schema or data-migration changes.
 */
const MIGRATION_VERSION = "2026.07.25.schema";

module.exports = { MIGRATION_VERSION };
